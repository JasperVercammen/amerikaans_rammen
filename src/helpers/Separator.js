import React, {Component} from 'react';
import {View} from 'react-native';

const Separator = () => {
  return (<View style={{height: 1, backgroundColor: '#E3E3E3', margin: 10, flex: 1}}/>);
};

export default Separator;